---
import { mainNavigation } from '../../data/navigation';
---

<footer class="bg-gray-950 text-gray-300">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 lg:py-16">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 lg:gap-12">
      <!-- Logo & Description -->
      <div class="lg:col-span-1">
        <a href="/" class="inline-block mb-4">
          <img
            src="/images/logos/therapysupport.png"
            alt="Therapy Support"
            class="h-10 w-auto"
          />
        </a>
        <p class="text-sm text-gray-400 leading-relaxed">
          Zintegrowana Platforma dla Terapeutów CBT. Nowy standard w psychoterapii.
        </p>
      </div>

      <!-- Navigation Links -->
      <div>
        <h3 class="text-white font-semibold text-sm mb-4">Nawigacja</h3>
        <ul class="space-y-2.5">
          {mainNavigation.slice(0, 4).map((item) => (
            <li>
              <a
                href={item.href}
                class="text-sm text-gray-400 hover:text-white transition-colors"
                {...(item.external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
              >
                {item.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div>
        <h3 class="text-white font-semibold text-sm mb-4">Informacje</h3>
        <ul class="space-y-2.5">
          {mainNavigation.slice(4).map((item) => (
            <li>
              <a
                href={item.href}
                class="text-sm text-gray-400 hover:text-white transition-colors"
                {...(item.external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
              >
                {item.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <!-- EU Notice -->
    <div class="mt-12 pt-8 border-t border-gray-800">
      <p class="text-xs text-gray-500 leading-relaxed max-w-4xl">
        Projekt &bdquo;Platforma Startowa Unicorn Hub &ndash; Edycja II&rdquo; współfinansowany ze środków Unii Europejskiej
        w ramach działania 1.1 Platformy Startowe dla nowych pomysłów Programu Fundusze Europejskie dla Polski Wschodniej 2021-2027.
      </p>
    </div>

    <!-- Copyright -->
    <div class="mt-6 pt-6 border-t border-gray-800 flex flex-col sm:flex-row justify-between items-center gap-4">
      <p class="text-xs text-gray-500">
        &copy; {new Date().getFullYear()} AI Therapy Support. Wszelkie prawa zastrzeżone.
      </p>
      <div class="flex flex-wrap items-center justify-center gap-4">
        <a href="/regulamin/" class="text-xs text-gray-500 hover:text-white transition-colors">
          Regulamin
        </a>
        <span class="text-gray-700">•</span>
        <a href="/polityka-prywatnosci/" class="text-xs text-gray-500 hover:text-white transition-colors">
          Polityka prywatności
        </a>
        <span class="text-gray-700">•</span>
        <a href="/cookies/" class="text-xs text-gray-500 hover:text-white transition-colors">
          Cookies
        </a>
        <span class="text-gray-700">•</span>
        <a href="/kontakt/" class="text-xs text-gray-500 hover:text-white transition-colors">
          Kontakt
        </a>
      </div>
    </div>
  </div>
</footer>

---
import Button from '../ui/Button.astro';
---

<section class="relative overflow-hidden bg-gradient-to-br from-gray-950 via-gray-900 to-gray-950 text-white">
  <!-- Floating decorative elements -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none" aria-hidden="true">
    <div class="absolute -top-24 -left-24 w-96 h-96 bg-sage-600/10 rounded-full blur-3xl animate-float"></div>
    <div class="absolute -bottom-24 -right-24 w-96 h-96 bg-sage-500/10 rounded-full blur-3xl animate-float" style="animation-delay: 1.5s;"></div>
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-sage-600/5 rounded-full blur-3xl"></div>
  </div>

  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-24 sm:py-32 lg:py-40">
    <div class="max-w-3xl mx-auto text-center">
      <!-- Subtitle badge -->
      <p class="inline-flex items-center px-4 py-1.5 rounded-full bg-sage-600/20 text-sage-400 text-sm font-medium mb-6">
        Nowy standard w psychoterapii
      </p>

      <!-- H1 -->
      <h1 class="text-4xl sm:text-5xl lg:text-6xl font-extrabold tracking-tight leading-tight">
        Zintegrowana Platforma dla Terapeutów
        <span class="text-sage-400">CBT</span>
      </h1>

      <!-- Description -->
      <p class="mt-6 text-lg sm:text-xl text-gray-300 leading-relaxed max-w-2xl mx-auto">
        Cała praca kliniczna w jednym, bezpiecznym miejscu: od wstępnej konsultacji
        i wywiadu, przez konceptualizację i plan terapii, aż po zestawianie notatek
        z sesji i raporty do superwizji, wszystko wspierane przez sztuczną inteligencję.
      </p>

      <!-- CTAs -->
      <div class="mt-10 flex flex-col sm:flex-row items-center justify-center gap-4">
        <Button
          variant="primary"
          size="lg"
          href="https://app.aitherapy.support/pricing"
          external
        >
          Rozpocznij okres próbny
        </Button>
        <Button
          variant="secondary"
          size="lg"
          href="https://cal.com/bartlomiej-glowacki-fe2ngd/therapy-support-demo-session"
          external
        >
          Umów się na sesję DEMO
        </Button>
      </div>
    </div>
  </div>
</section>

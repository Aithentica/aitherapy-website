---
interface StatCard {
  value: string;
  label: string;
}

const stats: StatCard[] = [
  { value: '100', label: 'sesji miesięcznie' },
  { value: '20', label: 'minut pracy niewidzialnej na 1 sesję' },
  { value: '20', label: 'godzin miesięcznie na administracji' },
];
---

<section class="py-20 lg:py-28 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="max-w-3xl mx-auto text-center mb-12 lg:mb-16">
      <!-- Tag -->
      <span class="inline-flex items-center px-3 py-1 rounded-lg border border-red-200 bg-red-50/50 text-red-600 text-xs font-semibold uppercase tracking-wider mb-4">
        Problem Terapeuty
      </span>

      <!-- H2 -->
      <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-[#181818]">
        Niewidzialna Praca
      </h2>

      <!-- Description -->
      <p class="mt-4 text-lg text-[#475569] leading-relaxed">
        Dokumentacja, archiwizacja, planowanie sesji, przygotowanie do superwizji,
        konceptualizacja, fakturowanie, SMS-y do pacjentów - to wszystko zabiera czas.
        <br /><br />My go oddajemy. Na terapię, rozwój i to, co napędza.
      </p>
      <p class="mt-4 text-lg text-[#475569] leading-relaxed">
        Zapytaliśmy. Codzienne rozmowy z terapeutami o ich pracy kształtują to, co budujemy.
      </p>
    </div>

    <!-- Statistics Grid -->
    <div class="grid grid-cols-1 sm:grid-cols-3 gap-6 lg:gap-8 max-w-4xl mx-auto">
      {stats.map((stat) => (
        <div class="text-center p-8 rounded-2xl bg-[#F8FAFC] border border-gray-100">
          <p class="text-5xl lg:text-6xl font-extrabold text-sage-900">{stat.value}</p>
          <p class="mt-2 text-[#475569] font-medium">{stat.label}</p>
        </div>
      ))}
    </div>
  </div>
</section>

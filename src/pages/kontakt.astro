---
import PageLayout from '../layouts/PageLayout.astro';
import Button from '../components/ui/Button.astro';
---

<PageLayout
  title="Kontakt"
  description="Skontaktuj sie z zespolem AI Therapy Support. Formularz kontaktowy, pytania o platforme i wspolprace."
>
  <!-- Hero -->
  <section class="relative bg-gray-950 text-white overflow-hidden">
    <div class="absolute inset-0 bg-gradient-to-br from-sage-600/20 to-transparent"></div>
    <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20 lg:py-28">
      <div class="max-w-3xl mx-auto text-center">
        <span class="inline-block px-4 py-1.5 bg-sage-600/20 text-sage-500 rounded-full text-sm font-medium mb-6">
          Kontakt
        </span>
        <h1 class="text-4xl sm:text-5xl font-extrabold leading-tight">
          Napisz do nas
        </h1>
        <p class="mt-6 text-lg sm:text-xl text-gray-300 leading-relaxed">
          Masz pytanie o platforme? Chcesz nawizac wspolprace? Napisz do nas — odpowiadamy w ciagu 24 godzin.
        </p>
      </div>
    </div>
  </section>

  <!-- Contact Form -->
  <section class="py-16 lg:py-24 bg-white">
    <div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8">
      <form
        action="https://formspree.io/f/xplaceholder"
        method="POST"
        class="space-y-6"
        id="contact-form"
      >
        <!-- Name -->
        <div>
          <label for="name" class="block text-sm font-medium text-gray-700 mb-2">
            Imie i nazwisko
          </label>
          <input
            type="text"
            id="name"
            name="name"
            required
            class="w-full px-4 py-3 rounded-xl border border-gray-200 text-gray-900 placeholder-gray-400 focus:ring-2 focus:ring-sage-600 focus:border-sage-600 outline-none transition-colors"
            placeholder="Jan Kowalski"
          />
        </div>

        <!-- Email -->
        <div>
          <label for="email" class="block text-sm font-medium text-gray-700 mb-2">
            Adres e-mail
          </label>
          <input
            type="email"
            id="email"
            name="email"
            required
            class="w-full px-4 py-3 rounded-xl border border-gray-200 text-gray-900 placeholder-gray-400 focus:ring-2 focus:ring-sage-600 focus:border-sage-600 outline-none transition-colors"
            placeholder="jan@przyklad.pl"
          />
        </div>

        <!-- Subject -->
        <div>
          <label for="subject" class="block text-sm font-medium text-gray-700 mb-2">
            Temat
          </label>
          <input
            type="text"
            id="subject"
            name="subject"
            required
            class="w-full px-4 py-3 rounded-xl border border-gray-200 text-gray-900 placeholder-gray-400 focus:ring-2 focus:ring-sage-600 focus:border-sage-600 outline-none transition-colors"
            placeholder="Pytanie o platforme"
          />
        </div>

        <!-- Message -->
        <div>
          <label for="message" class="block text-sm font-medium text-gray-700 mb-2">
            Wiadomosc
          </label>
          <textarea
            id="message"
            name="message"
            required
            rows="6"
            class="w-full px-4 py-3 rounded-xl border border-gray-200 text-gray-900 placeholder-gray-400 focus:ring-2 focus:ring-sage-600 focus:border-sage-600 outline-none transition-colors resize-y"
            placeholder="Twoja wiadomosc..."
          ></textarea>
        </div>

        <!-- Honeypot (anti-spam) -->
        <input type="text" name="_gotcha" style="display:none" />

        <!-- Submit -->
        <div>
          <button
            type="submit"
            class="w-full sm:w-auto inline-flex items-center justify-center px-8 py-3.5 rounded-full bg-sage-600 text-white font-semibold text-sm hover:bg-sage-700 transition-colors shadow-lg shadow-sage-600/25 hover:shadow-sage-600/40"
          >
            Wyslij wiadomosc
          </button>
        </div>

        <!-- Success Message (hidden by default) -->
        <div id="form-success" class="hidden p-4 rounded-xl bg-sage-50 border border-sage-200 text-sage-700">
          <p class="font-semibold">Dziekujemy za wiadomosc!</p>
          <p class="text-sm mt-1">Odpowiemy w ciagu 24 godzin.</p>
        </div>

        <!-- Error Message (hidden by default) -->
        <div id="form-error" class="hidden p-4 rounded-xl bg-red-50 border border-red-200 text-red-700">
          <p class="font-semibold">Wystapil blad</p>
          <p class="text-sm mt-1">Prosimy sprobowac ponownie lub napisac bezposrednio na nasz adres e-mail.</p>
        </div>
      </form>
    </div>
  </section>

  <!-- Additional Contact Info -->
  <section class="py-16 lg:py-24 bg-gray-50">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-md mx-auto">
        <div class="bg-white rounded-2xl border border-gray-100 p-8 shadow-sm text-center">
          <div class="inline-flex items-center justify-center w-12 h-12 rounded-xl bg-sage-50 text-sage-600 mb-4">
            <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" d="M21.75 6.75v10.5a2.25 2.25 0 0 1-2.25 2.25h-15a2.25 2.25 0 0 1-2.25-2.25V6.75m19.5 0A2.25 2.25 0 0 0 19.5 4.5h-15a2.25 2.25 0 0 0-2.25 2.25m19.5 0v.243a2.25 2.25 0 0 1-1.07 1.916l-7.5 4.615a2.25 2.25 0 0 1-2.36 0L3.32 8.91a2.25 2.25 0 0 1-1.07-1.916V6.75" />
            </svg>
          </div>
          <h3 class="text-lg font-bold text-gray-900 mb-2">E-mail</h3>
          <p class="text-gray-600">Wolisz napisac bezposrednio?</p>
          <a href="mailto:kontakt@aitherapy.support" class="text-sage-600 hover:text-sage-700 font-medium mt-2 inline-block">
            kontakt@aitherapy.support
          </a>
        </div>
      </div>
    </div>
  </section>
</PageLayout>

<script is:inline>
  const form = document.getElementById('contact-form');
  const successMsg = document.getElementById('form-success');
  const errorMsg = document.getElementById('form-error');

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    successMsg.classList.add('hidden');
    errorMsg.classList.add('hidden');

    try {
      const response = await fetch(form.action, {
        method: 'POST',
        body: new FormData(form),
        headers: { 'Accept': 'application/json' },
      });

      if (response.ok) {
        form.reset();
        successMsg.classList.remove('hidden');
      } else {
        errorMsg.classList.remove('hidden');
      }
    } catch {
      errorMsg.classList.remove('hidden');
    }
  });
</script>
